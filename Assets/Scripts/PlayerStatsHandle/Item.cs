using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item
{
/*    public void Equip(PlayerData c)
    {
        c.Strength.AddModifier(new StatModifier(10, StatModType.Flat, this));
        c.Strength.AddModifier(new StatModifier(0.1f, StatModType.Flat, this));
    }

    public void Unequip(PlayerData c)
    {
        c.Strength.RemoveModifiersFromSource(this);
    }*/
}
